name = "xauusd-zona-kuzatuv"
main = "worker-index.js"
compatibility_date = "2025-05-01"

# Cron — har 15 daqiqada
[triggers]
crons = ["*/15 * * * *"]

# KV binding — namespace "xauusd-zona-state" (Claude yaratdi).
[[kv_namespaces]]
binding = "ZONE_STATE"
id = "5a3997360ba94514b5b6c0a09e25c8a6"

# ESLATMA: TODOIST_TOKEN va TWELVE_KEY — SECRET.
# Ular allaqachon worker'da o'rnatilgan va deploy'da o'chmaydi.
# wrangler.toml ga YOZMANG (maxfiy). Kerak bo'lsa: Settings > Variables and Secrets.
